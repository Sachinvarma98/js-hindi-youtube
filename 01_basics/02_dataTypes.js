"use strict"; // treat all JS code as newer version

// alert(3+3)           // we are using nodejs, not browser

// Primitive data types

let name = "Sachin"  // String data type
let age = 21          // Number data type
let isLoggedIn = true   // Boolean data type
let state;              // Undefined
let district = null;     // Null maens empty.      // defined as an object

console.log(typeof "Sachin");
console.log(typeof 21);
console.log(typeof true);
console.log(typeof null);
console.table([ name, age, isLoggedIn, state, district])